{
  "name": "@followr-mcp/worker",
  "version": "0.3.3",
  "private": true,
  "description": "Cloudflare Worker for Followr MCP remote HTTP transport (v0.2 placeholder).",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@followr-mcp/mcp-core": "0.3.3",
    "@followr-mcp/shared": "0.3.3",
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240924.0"
  }
}
