{
  "name": "followr-mcp-root",
  "version": "0.6.1",
  "private": true,
  "description": "Followr MCP server. Connects Claude and other AI agents to Followr's social media management platform.",
  "workspaces": [
    "packages/shared",
    "packages/mcp-core",
    "packages/stdio",
    "packages/worker"
  ],
  "scripts": {
    "build": "tsc --build packages/shared packages/mcp-core && npm run build --workspace=@followr/mcp && node scripts/copy-assets.mjs",
    "build:worker": "cd packages/worker && npx wrangler deploy --dry-run --outdir dist",
    "typecheck": "node scripts/typecheck.mjs",
    "clean": "rm -rf packages/*/dist packages/*/tsconfig.tsbuildinfo",
    "test": "tsc --build packages/shared packages/mcp-core && vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "gpt-tokenizer": "^3.4.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.7"
  },
  "overrides": {
    "qs": "^6.15.2"
  },
  "license": "MIT"
}
